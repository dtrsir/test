<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhong.dao.BookMapper">

    <insert id="addBook" parameterType="books">
        insert into ssmbuild.books(bookName, bookCounts, detail)
        values (#{bookName}, #{bookCounts}, #{detail});
    </insert>

    <delete id="deleteBookById" parameterType="_int">
        delete from ssmbuild.books where bookID = #{id};
    </delete>

    <update id="updateBook" parameterType="books">
        update ssmbuild.books
        <set>
            <if test="bookName != null">
                bookName = #{bookName},
            </if>
            <if test="bookCounts != null">
                bookCounts = #{bookCounts},
            </if>
            <if test="detail != null">
                detail = #{detail},
            </if>
        </set>
        where bookID = #{bookID};
    </update>

    <select id="queryBook" parameterType="map" resultType="books">
        select *
        from ssmbuild.books
        <where>
            <if test="bookID != null">
                and bookID = #{bookID}
            </if>
            <if test="bookName != null">
                and bookName like  concat('%',#{bookName},'%')
            </if>
            <if test="bookCounts != null">
                and bookCounts between 0 and #{bookCounts}
            </if>
        </where>
    </select>

    <select id="queryBookById" resultType="books" parameterType="_int">
        select *
        from ssmbuild.books
        where bookID = #{id};
    </select>

    <select id="queryAllBook" resultType="books">
        select *
        from ssmbuild.books;
    </select>

    <select id="countBook" resultType="map" parameterType="_int">
        select COUNT(1)
        from ssmbuild.books;
    </select>


</mapper>