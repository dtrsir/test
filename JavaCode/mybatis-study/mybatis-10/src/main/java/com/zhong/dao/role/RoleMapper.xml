<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhong.dao.role.RoleMapper">

    <!--获取角色列表-->
    <select id="getRoleList" resultType="role">
        select * from smbms.smbms_role;
    </select>

    <!--增加角色信息-->
    <insert id="add" parameterType="role">
        insert into smbms.smbms_role(id, roleCode, roleName, createdBy, creationDate)
        values (#{id}, #{roleCode}, #{roleName}, #{createdBy}, #{creationDate});
    </insert>

    <!--通过Id删除Role-->
    <delete id="deleteRoleById">
        delete from smbms.smbms_role where id = #{id};
    </delete>

    <!--修改角色信息-->
    <update id="modify" parameterType="role">
        update smbms.smbms_role 
        <set>
            <if test="roleCode != null">
                roleCode = #{roleCode},
            </if>
            <if test="roleName != null">
                roleName = #{roleName},
            </if>
            <if test="modifyBy != null">
                modifyBy = #{modifyBy},
            </if>
            <if test="modifyDate != null">
                modifyDate = #{modifyDate},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过Id获取role-->
    <select id="getRoleById" resultType="role">
        select * from smbms.smbms_role where id = #{id};
    </select>

    <!--进行角色编码的唯一性验证-->
    <select id="roleCodeIsExist" resultType="_int">
        select count(1) from smbms.smbms_role where roleCode = #{roleCode};
    </select>

</mapper>